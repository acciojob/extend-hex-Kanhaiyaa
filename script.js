const extendHex = (shortHex) => {
  // write your code here
	let s="#abc"
	if (s.length===4) {
		let ans=`# ${s[i]}${s[i]} ${s[2]}${s[2]} ${s[3]}${s[3]}`
		console.log(ans)
	}
	
};

// Do not change the code below.
// const shortHex = prompt("Enter Short Hex.");
// alert(extendHex(shortHex));






















